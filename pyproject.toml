[project]
name = "demo-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask==3.0.0",
    "werkzeug==3.0.1",
]

[tool.uv.workspace]
members = [
    "agent",
]
