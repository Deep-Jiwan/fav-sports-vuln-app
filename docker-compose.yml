version: '3.8'

services:
  sports-tracker:
    build: .
    container_name: sports-tracker-app
    ports:
      - "3000:3000"
    environment:
      - SECRET_KEY=sports_tracker_2024_secret
    restart: unless-stopped
